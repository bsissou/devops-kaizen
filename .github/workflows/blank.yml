name: CI Kaizen

on: [push]

jobs:
  docker-test:
    runs-on: ubuntu-latest
    container: node:18

    steps:
         - name: Voir les fichiers
           run: ls -la

         - name: Vérifier version Node
           run: node -v
         - name: message final
           run: echo " Pipeline ok"
